<% if user_signed_in? %>
	<h1>You're logged in</h1>
<% else %>

<div class="hero-unit">
		<div style="text-align: center; margin-bottom: 25px;"><%= DateTime.now.to_date.strftime("It's %A") %></div>
	  <h1>Organize yourself.</h1>
		<p style="margin-top: 50px;">
			<%= link_to "Sign Up", new_user_registration_path, class: "button" %>
		</p>
</div>

<% end %>